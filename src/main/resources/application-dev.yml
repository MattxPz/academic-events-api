spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/academic_events_db
    username: ups
    password: ups123
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true 

logging:
  level:
    ec.edu.ups.academicevents: DEBUG